export const metadata = {
  title: 'Football Predictions - SokaPulse',
  description: 'Get accurate football predictions, tips, and statistics for upcoming matches.',
};

export default function PredictionsLayout({ children }) {
  return <>{children}</>;
}