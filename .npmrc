# Speed up builds by disabling audit
audit=false
# Disable fund messages
fund=false
# Increase network timeout for slower connections
network-timeout=60000
# Disable progress bar for cleaner logs
progress=false
# Production mode for cleaner installs
production=false
# Don't generate a lockfile
package-lock=true 